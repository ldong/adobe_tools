var layer = app.activeDocument.activeLayer;var bounds = layer.bounds;$.writeln(bounds);$.writeln(layer.name);    try {        var doc = app.activeDocument;        var pathItem = null;        for (var i= 0, len = doc.pathItems.length; i < len; i++) {            var p = doc.pathItems[i];            if (p.name.indexOf(layer.name) > -1) {                pathItem = p;                break;            }        }        if (pathItem != null && pathItem.subPathItems.length > 0) {            var subItem = pathItem.subPathItems[0];            if (subItem.closed && subItem.pathPoints.length > 0) {                //$.writeln(subItem.pathPoints);                return calRadius(subItem.pathPoints);            }        }    } catch (e) {        $.writeln("err: " + e.toString());    }    function calRadius(points) {        for (var i=0; i<points.length; i++) {            $.writeln(points[i]);        }        return -1;    }